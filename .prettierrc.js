module.exports = {
    semi: true, // Poner punto y coma al final de las sentencias.
    trailingComma: 'es5', // Poner comas finales donde sea válido en ES5 (objetos, arrays, etc.).
    singleQuote: true, // Usar comillas simples en lugar de dobles.
    printWidth: 80, // Intentar que las líneas no excedan los 80 caracteres.
    tabWidth: 2, // Usar 2 espacios por tabulación.
    // bracketSpacing: true, // Espacio entre llaves en literales de objeto: { foo: bar } (default)
    // arrowParens: 'always', // Siempre paréntesis alrededor de los parámetros de arrow functions: (x) => x (default)
};